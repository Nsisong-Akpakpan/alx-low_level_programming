/*
 * File: 2-strlen.c
 */

#include "main.h"
#include <string.h>
/**
 *_strlen - Returns the length of a string.
 * @s: The string to get the length of.
 * Return: The length of string.
 */
int _strlen(char *s)
{
	return (strlen(s));
}
